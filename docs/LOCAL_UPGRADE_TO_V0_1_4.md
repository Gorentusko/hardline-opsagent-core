# Local Upgrade to v0.1.4

If you already committed v0.1.3 locally, copy the v0.1.4 files over that repo, then run:

```powershell
git status
git add .
git commit -m "Polish public GitHub release v0.1.4"
```

Then push later when GitHub login is ready.

